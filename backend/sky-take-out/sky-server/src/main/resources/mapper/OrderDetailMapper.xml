<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">


    <insert id="insertBatch">
        insert into order_detail (name, image, setmeal_id, dish_id, dish_flavor, amount, number, order_id)
        values
        <foreach collection="orderDetailList" item="orderDetail" separator=",">
            (#{orderDetail.name}, #{orderDetail.image}, #{orderDetail.setmealId}, #{orderDetail.dishId}, #{orderDetail.dishFlavor}, #{orderDetail.amount}, #{orderDetail.number}, #{orderDetail.orderId})
        </foreach>
    </insert>
</mapper>
